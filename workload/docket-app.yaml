name: workload
version: "1"
description: Who is carrying what, and how much of it — the report a capacity app sells
surfaces:
  pages:
    - name: workload
      title: Workload
      run: hooks/workload.sh
